import express from "express";
// import { createURL, validateURL, validateUser, getLinks } from "./qrcode.controller.js";
const qrcodeRouter = express.Router();

// qrcodeRouter.post('/', validateURL, validateUser, createURL);
// qrcodeRouter.get('/', validateUser, getLinks);

export { qrcodeRouter };
